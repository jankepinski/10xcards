---
import AuthLayout from "../layouts/AuthLayout.astro";
import PasswordResetForm from "../components/auth/PasswordResetForm";

// This would normally come from server-side processing or API
const isLoading = false;
const error = null;
const success = false;

// Temporary function until backend is implemented
const handlePasswordReset = () => {
  console.log("Password reset attempted with:", { password: "********", confirmPassword: "********" });
  // Backend implementation will be added later
};
---

<AuthLayout title="Reset Password">
  <PasswordResetForm client:load onSubmit={handlePasswordReset} isLoading={isLoading} error={error} success={success} />
</AuthLayout>
