---
import AuthLayout from "../layouts/AuthLayout.astro";
import RegisterForm from "../components/auth/RegisterForm";

// This would normally come from server-side processing or API
const isLoading = false;
const error = null;

// Temporary function until backend is implemented
const handleRegister = (email: string, password: string, confirmPassword: string) => {
  console.log("Registration attempted with:", email, password, confirmPassword);
  // Backend implementation will be added later
};
---

<AuthLayout title="Create Account">
  <RegisterForm client:load onSubmit={handleRegister} isLoading={isLoading} error={error} />
</AuthLayout>
