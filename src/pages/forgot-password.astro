---
import AuthLayout from "../layouts/AuthLayout.astro";
import PasswordRecoveryForm from "../components/auth/PasswordRecoveryForm";

// This would normally come from server-side processing or API
const isLoading = false;
const error = null;
const success = false;

// Temporary function until backend is implemented
const handlePasswordRecovery = (email: string) => {
  console.log("Password recovery attempted for:", email);
  // Backend implementation will be added later
};
---

<AuthLayout title="Forgot Password">
  <PasswordRecoveryForm
    client:load
    onSubmit={handlePasswordRecovery}
    isLoading={isLoading}
    error={error}
    success={success}
  />
</AuthLayout>
